# 交互设计打磨（UX · 功能如何呈现、UI 如何衔接）

> **定位**：与 PRD 互补——PRD 定"做什么、功能点"，本文定"**如何呈现、页面间如何衔接**"。
> **核心**：个人日程（周览记事本）是移动端主界面的设计主干；客户管理等为支撑模块。
> 配套：`docs/PRD.md`（功能点/验收）、`docs/技术打磨清单.md`（实现健壮性）。

---

## 1. 交互设计基线（跨模块通用）

### 1.1 页面分区（详情页三段式）

```
PageHeader（标题 + 主操作区，主操作靠右）
  ├─ 主信息区（谁/是什么：名称、等级、状态、负责人）
  ├─ 业务操作区（记录入口：记拜访/建商机/登客诉 → 明显可点）
  └─ 时间线区（历史活动：按时间聚合，只读）
```

**原则**：主操作一眼可见；危险操作（释放/结案/作废）与主操作视觉分离（红/灰）；编辑等次要操作收敛到"更多"或图标。

### 1.2 操作组织

- **主操作**（主色）：新建、提交、认领
- **次要操作**（灰/描边）：编辑、移交、申请接管
- **危险操作**（红色/描边）：释放、结案、作废、停用 → 必须弹确认 + 说明后果
- 入口按能力点显隐（`hasAbility`），无权限不渲染

### 1.3 衔接原则

| 场景                         | 衔接方式                                                               |
| ---------------------------- | ---------------------------------------------------------------------- |
| 列表 → 详情                  | 行点击跳详情（web）；卡片点击（mobile）                                |
| 详情 → 填报表单              | **弹窗/抽屉（web）或 push 页面（mobile）**，完成后返回详情并刷新时间线 |
| 表单提交成功                 | toast + 返回来源页 + 目标页刷新（红线 11，不整页刷新）                 |
| 弹窗类操作（移交/接管/释放） | 弹窗内完成 → 关闭 → 详情局部刷新（`reload`），不跳走                   |
| 返回                         | mobile `router.back()`；web 详情页"返回列表"（保持查询上下文，后置）   |

### 1.4 双端一致性

- 重合功能（客户详情/表单）**结构对齐**：mobile 的"快速登记"组 ↔ web 的"业务操作区"必须同构（§5.4 schema 共享）；
- 差异只在形态（web 弹窗 vs mobile 整页 push），**功能入口一个不少**。

---

## 2. 个人日程：周览记事本（核心界面）

> 心智：**记事本 / 日历**——过去是账本（干了啥），未来是计划（要做啥），逾期是"该做没做"。
> 呼应蓝图：§8.7 周窗口、§8.10 日 card 待办聚合、§8.4 下次跟进自动落位。

### 2.1 定位

- **移动端主界面**：销售打开看"今天/本周要干什么 + 干了啥"（P0 体验核心）；
- **桌面端"周计划"页**：侧边栏入口，个人视角（不是主界面，桌面主界面另行设计）。

### 2.2 移动端周览（单列日 card 流）

- 布局：周选择器 + **7 张日 card 纵向滚动**；默认定位今天，今天高亮
- 每张 card 内**类型分区**（区域分明）：计划 / 拜访 / 商机跟进 / 客诉跟进
- **过去周只读**（历史账本）；本周/未来可编辑（计划）；**逾期标红置顶**
- 空 card 也有"+ 记一笔"入口；点某类型分区 → 直接进对应表单（预填该日期）
- 单列适合单手滑动，一屏 1~2 天

### 2.3 桌面端周览（横向周网格）

- 布局：周选择器 + **7 列网格（一屏一周）**，周一~周日横排
- 每列内分区/色块（计划 / 拜访 / 商机 / 客诉）；**hover 预览、点击弹窗**（填表或查看）
- 大屏可并列"计划 vs 实际"（§8.7 分列展示，不精确配对）
- 过去只读 / 未来可编辑 / 逾期标红，与移动端一致

### 2.4 计划与记录分流（关键原则）

| 动作                       | 思维                      | 交互                                                  |
| -------------------------- | ------------------------- | ----------------------------------------------------- |
| **排计划**（未来计划项）   | "哪天干什么" → **按日期** | **日历式：点某天空白 → 快速加计划项**（桌面尤其丝滑） |
| **记录**（拜访/商机/客诉） | "记一个拜访" → **按内容** | 类型驱动：走"记一笔"入口                              |

> 加计划=按天思维（点哪天加哪）；记记录=按内容思维（内容是核心，日期是属性）。两边不混。

### 2.5 联动落位

| 动作                 | 落位                                                |
| -------------------- | --------------------------------------------------- |
| 拜访填"下次拜访"     | 自动在该日期 card 生成计划项（§8.4 强一致，同事务） |
| 商机推进填"下次跟进" | 落对应日 card                                       |
| 客诉跟进填"下次确认" | 落对应日 card                                       |
| 计划项执行           | 点计划项 → 直接进拜访表单（预填客户+日期）          |
| 完成/解决            | 标记完成/打勾置灰，保留可见（历史回溯）             |

## 3. 填报便利性（移动端，P0）

### 3.1 默认今天

- 周览默认定位今天；"记一笔"默认今天；表单日期字段默认今天
- 改日期是**例外动作**，不打断"打开即填"

### 3.2 补录快捷（外勤刚需）

- 日期字段提供 **"昨天 / 今天 / 明天"快捷按钮**，点一下切换，不翻日历
- 覆盖"现场没空记、晚上补录"的高频场景

### 3.3 多入口预填

| 入口            | 预填          |
| --------------- | ------------- |
| Tabbar·记一笔   | 默认今天      |
| 周览·某天分区   | 该日期        |
| 客户详情·记拜访 | 该客户 + 今天 |

### 3.4 轻量表单

- mobile 只留必填（客户 + 时间 + 方式 + 一句话），复杂字段收起
- 呼应 P0"顺手快不打断"

## 4. 填报入口（类型面板 · 学 Tabbar）

### 4.1 移动端"记一笔"Tab

- 快捷面板：**拜访 / 商机 / 客诉**三大按钮 + 顶部默认日期（可切昨今天），点哪个进哪个表单（超兔式：意图直接）

### 4.2 桌面端填报入口

- 周览 cell 点击 → **弹窗**填表（预填日期，web 弹窗/抽屉，mobile 才整页 push）
- 侧边栏各模块列表的"新建"按钮（类型入口）
- 客户详情"记拜访"（预填客户）

---

## 5. 支撑模块 · 客户管理链路

### 5.1 客户列表

- 呈现：卡片式/表格均可用；关键列（名称/城市/等级/状态/负责人）；状态用色块标签
- 衔接：点行 → 详情；"新建客户"主按钮（`customer.write`）

### 5.2 新建客户（查重弹窗交互）

- 流程：填名称+电话 → 提交时**静默查重**：
  - 硬拦截 → 直接报"已存在"，禁止继续
  - 疑似 → 弹窗列出疑似客户（名称+城市+置信度徽标），三个动作：**查看疑似 / 继续新建 / 申请接管**
- 衔接：继续新建 → 原表单保留不丢失；成功后跳详情
- 内联建档：客户选择器搜不到 → 当前表单内展开快速建档（名称+电话），不跳页

### 5.3 客户详情（现有问题 → 目标设计）

**现有问题（代码实测）**：

| 问题                | 现状                                                                              |
| ------------------- | --------------------------------------------------------------------------------- |
| web 缺填报入口      | 详情只有移交/释放/申请接管，**没有记拜访/建商机/登客诉**（mobile 有"快速登记"组） |
| 移交用"用户 ID"输入 | 弹窗让用户填对方 ID（没人知道），应换成**用户选择器**                             |
| 无活动时间线        | 详情只有基本信息+联系人，**没有拜访/商机/客诉的时间线聚合**（PRD 4.3）            |
| web 无编辑入口      | 找不到"编辑客户"按钮（PRD 4.4）                                                   |

**目标设计**：

```
头部：客户名（+ 状态/等级标签）     操作区：记拜访(主) | 商机 | 客诉 | 编辑 | 移交/接管/释放(危险)
基本信息卡：descriptions（名称/城市/产业/等级/状态/负责人/地址/备注）
业务操作区：三个记录入口卡片（图标+文案，mobile 已有，web 补齐）
联系人卡：列表 + 添加/编辑（首要联系人徽标）
时间线：拜访/商机/客诉按时间倒序聚合（类型图标 + 摘要 + 时间）
```

### 5.4 归属操作衔接

| 操作         | 交互设计                                                                          |
| ------------ | --------------------------------------------------------------------------------- |
| **移交**     | 弹窗：**用户选择器**（搜索显示名）+ 原因；确认后 toast + 详情刷新（负责人列变化） |
| **接管申请** | 弹窗：原因；提交后 toast + 提示"已提交审批"                                       |
| **释放**     | 确认弹窗：去向（公海/无效）+ 原因 + **后果说明**（未解决客诉拦截提示）            |
| **认领**     | 主按钮直接执行 + toast                                                            |

- 所有归属操作**弹窗内完成**，不跳走详情（衔接原则 1.3）

### 5.5 双端一致性清单

- ☐ web 详情补齐"业务操作区"（与 mobile"快速登记"同构）
- ☐ 移交选择器双端统一（web 弹窗 / mobile 弹层 picker）
- ☐ 时间线双端呈现（web 表格时间线 / mobile cell 列表）
- ☐ 归属操作双端入口一致（web 完整 / mobile 查看为主 + 关键操作）

---

## 6. 商机闭环（管道看板 · 借鉴 Pipedrive）

### 6.1 定位

- 商机的核心动作是**推进阶段**；桌面端用看板让阶段一目了然（Pipedrive 验证过的做法）

### 6.2 桌面端：阶段看板

- 列：intent → following → ordered / lost / demand_disappeared（§8.5 状态机）
- 卡片：客户 + 名称 + 意向金额 + 下次跟进（**逾期红标**）
- 点卡片 → **弹窗推进**（结论 + 下一步，可转订单/结案）
- 拖拽为可选增强（小团队以按钮推进为主，不强制拖拽）

### 6.3 移动端：列表 + 详情推进

- 列表：按阶段筛选 / 逾期优先
- 详情：基本信息 + **事件时间线**（created/stage_changed/updated）+ 推进按钮（不拖拽）

### 6.4 逾期治理

- 逾期商机：看板列顶红标 / 列表置顶
- **点进去即处置**（推进更新日期 / 结案），不是只看

## 7. 客诉闭环（工单式 · 借鉴 Zendesk）

### 7.1 定位

- **待解决驱动**：客诉的价值在"解决"，界面围绕"还有哪些没解决 + 逾期"

### 7.2 列表

- 逾期 / 未解决优先排序，逾期红点
- 状态标签（registered / resolved）+ 解决后置灰

### 7.3 详情

- 问题描述 + 发生时间 + **跟进时间线（只追加）** + 下次确认日
- 操作：跟进（内容必填；FOLLOWED_UP → 下次确认日；RESOLVED → 解决结果）
- 解决为终态，跟进入口置灰/拦截（§8.6）

### 7.4 联动

- 未解决 + 逾期 → 风险看板（§8.9 客户风险）

## 8. 费用（随手记 · 借鉴记账类应用）

### 8.1 定位

- 轻量统计，非报销（蓝图 §8.8）；"顺手记"不折腾

### 8.2 移动端：快速记一笔

- 默认今天 + 五类金额（烟酒/礼品/餐叙/招待/住宿）快速输入 + 备注 + 提交
- 同天再记 = 更新原记录（upsert，§8.8）

### 8.3 桌面端：月度列表 + 汇总

- 月度列表（按人/月/分项）；汇总卡（个人按日、团队按月）
- 三态可见：draft（草稿灰）/ submitted（计入）/ voided（剔除标灰留痕）

### 8.4 作废

- 确认弹窗："将不再计入统计，留痕可查" → 提交

## 9. 周计划深度（日历/任务式 · 借鉴 Todoist/滴答）

### 9.1 定位

- 周览之上：计划项管理 + 计划 vs 实际 + 指导意见闭环

### 9.2 计划项管理

- 增删、调整日期、标注完成；拜访联动生成的计划项有来源标记

### 9.3 计划 vs 实际（桌面）

- 周网格内并列"计划"与"实际"两行（§8.7 分列展示，不精确配对）

### 9.4 指导意见

- 管理者 → 被指导者，**红点已读闭环**；author 须为被指导者上级（§8.7）

## 10. 登录 / 用户管理（标准 SaaS）

### 10.1 登录

- 标准登录页；会话保持（refresh 静默续期）；403 引导（无权限提示）
- 防爆破：错误 5 次锁定提示（后端语义，前端展示）

### 10.2 用户管理

- 表格 + 行操作（编辑 / 停用启用 / 重置密码），交互见 PRD §3
- 操作按 `user.manage` 显隐；停用/重置有确认 + 后果说明

## 11. 桌面工作台（Dashboard · 借鉴 HubSpot）

### 11.1 定位

- 桌面登录落地页；回答"今天要做什么、有什么风险"

### 11.2 组成

- **今日待办卡片**：今天的计划项 + 到期商机/客诉（红点，点击直达处置）
- **快捷入口**：记拜访 / 建商机 / 登客诉（customer.write）
- **风险提醒**：逾期客诉 / 大商机停滞（executive）
- **最近活动流**：最近的拜访/商机/客诉动态（时间线摘要）

---

_本文是"如何呈现/如何衔接"的设计基准；周览与各模块交互设计已定稿，可进入 code（按模块逐块实现并对照 PRD 验收）。_
